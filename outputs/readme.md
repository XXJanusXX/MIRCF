# Outputs Directory

This directory is populated automatically when running:

- `make reproduce`
- GitHub Actions CI (`ci-notebook.yml`)

Expected contents include:
- RQ1–RQ6 figures (SVG + JPG)
- Table figures (Fig 6.8, 6.9)
- Optional executed notebook (CI only)

This directory should be empty except for this README in the repository.

